spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
logging:
  level:
    io.github.lumklar.server: debug
    org.hibernate.SQL: DEBUG
